### Minimal runtime for the Streamlit dashboard (optimized for Hugging Face Spaces)
# Pin to wheels that are available on Linux/py3.10 builders for fast, reliable deploys
streamlit==1.38.0
plotly==5.24.1
pandas==2.2.3
numpy==2.1.3
python-dotenv==1.0.0

# Optional: speeds up Parquet/Arrow usage in Streamlit (used internally by Streamlit)
pyarrow==22.0.0
